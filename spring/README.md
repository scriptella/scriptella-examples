Spring integration example
=======================

This example shows how to call Scriptella 1.4 in a Spring 5.3 application with
a configured H2 DataSource. Usage of Maven is also demonstrated.

Run `mvn test` with Java 17 to run `com.github.scriptella.examples.SpringIntegrationTest`.
As a result, an in-memory database will be created and 10 records added.
